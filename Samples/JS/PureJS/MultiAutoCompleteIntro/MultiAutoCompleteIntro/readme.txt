MultiAutoComplete
------------------------------------------------------------------------------
Shows how to perform common tasks with the MultiAutoComplete control.

The MultiAutoComplete control has the same features as the AutoComplete control,
but it allows users to select multiple items instead of one.

